制作一个基于leaflet的平面地图(simple crs)标点系统。 要求如下：
技术栈: pnpm+react+vite+shadcn(--preset b6F9Pikvg)，其余需要的技术栈自行决定
自行通过git管理项目。

功能如下：
1. 可以创建项目（支持多个项目自由切换，每个项目有名称和简介）
2. 项目内是一个地图，可以创建多个楼层，每个楼层支持上传一张底图（可以强制约束底图的坐标大小，如果不同的话就不允许上传），底图支持svg/png等多种格式
3. 在楼层上可以叠加其他图层来标点，一共支持以下几种：
    1. 单一一个点
    2. 多边形
    3. 路线声明（此处支持创建边和点，边的端点可以互相吸附，也可以和特征点吸附。它们会共同组成一个图，可以直接导出）（边也支持连接到其他图层的点）
4. 每个图层都可以选择一个数据约束（例如点可以有id和name两个属性，必填，支持图形化设置哪个是哪个）
5. 支持最终将所有数据批量导出（每个楼层每个图层为一个geoJson文件），也可以导出一个压缩包，包括当前项目的所有配置，其他人可以轻松通过导入这个压缩包来达到相同的工作环境

如果还有什么需求是含糊不清，或者较难实现的，请问我问题，我们完善需求。
